# Copy to .env and fill in for your machine.  (.env is gitignored.)
# Get your IDs with:   id -u    and    id -g
HOST_UID=1000
HOST_GID=1000

# Host dir where Claude's workspace + outputs live.
CLAUDE_WORK_DIR=/home/you/claude-work
